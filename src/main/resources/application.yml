spring:

  application:
    name: food

  h2:
     console:
        enabled: true

  datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:~/opensw
      username: sa
      password:

  jpa:
      show-sql: true
      database-platform: org.hibernate.dialect.H2Dialect
      properties:
        hibernate:
          format_sql: true
          show_sql: true

      hibernate:
        ddl-auto: update

jwt:
    secret: A2967C53C9F5AA135BD6A5198E183
    expiration: 86400000 # 임시 만료 시간 설정 추후 변경 (24시간)